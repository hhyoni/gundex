# GunDex Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 총기 모델을 분류·검색·상세보기·비교할 수 있는 교육용 도감 앱(Expo)을 만든다.

**Architecture:** 데이터(`data/guns.json`)와 화면을 분리한다. 화면은 데이터 함수(`lib/gunData.js`)만 호출하므로, 나중에 JSON을 위키 API로 교체해도 화면 코드는 바뀌지 않는다. 비교 기능은 순수 로직(`lib/compareLogic.js`) 위에 React Context(`context/CompareContext.js`)를 얹어 앱 전체에서 공유한다. 화면 이동은 React Navigation의 루트 스택(탭 + 상세) 구조를 쓴다.

**Tech Stack:** Expo (React Native), JavaScript, React Navigation (bottom-tabs + native-stack), Jest + jest-expo + @testing-library/react-native.

## Global Constraints

- 언어: JavaScript (TypeScript 아님). 모든 파일 확장자는 `.js`.
- 데이터 출처 분리: 화면은 절대 `guns.json`을 직접 import하지 않는다. 오직 `lib/gunData.js`의 함수만 호출한다.
- 분류 코드값 6종 고정: `pistol`, `rifle`, `shotgun`, `smg`, `sniper`, `machinegun`.
- 스키마 필드 13개: `id, name, manufacturer, country, category, caliber, weight, length, year, description, imageUrl, imageCredit, sourceUrl`.
- `weight`, `length`, `caliber`는 단위를 포함한 문자열. `year`만 숫자.
- 사진은 Wikimedia Commons, 출처는 `sourceUrl`/`imageCredit`에 반드시 채운다.
- 비교함은 최대 2개.
- 모든 커밋 메시지는 한국어로 간단히.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `package.json`, `app.json`, `App.js` | Expo 진입점, 네비게이션 + Provider 조립 |
| `data/guns.json` | 총기 데이터 배열 (시드 8개) |
| `data/categories.js` | 분류 코드값 ↔ 한글 표시값 매핑 |
| `lib/gunData.js` | 데이터 접근 함수 (전체/분류/검색/단건) |
| `lib/compareLogic.js` | 비교함 순수 로직 (담기/빼기/포함여부, 최대 2) |
| `context/CompareContext.js` | 비교함 상태를 앱 전체에 공유 |
| `components/GunCard.js` | 목록/검색에서 재사용하는 한 줄 카드 |
| `screens/ListScreen.js` | 분류별 목록 화면 |
| `screens/SearchScreen.js` | 검색 화면 |
| `screens/DetailScreen.js` | 상세 화면 |
| `screens/CompareScreen.js` | 비교 화면 |

테스트는 각 소스 파일 옆에 `*.test.js`로 둔다.

---

### Task 1: 프로젝트 셋업 (Expo + Jest)

**Files:**
- Create: `package.json`, `app.json`, `App.js`, `babel.config.js` (Expo가 생성)
- Test: `smoke.test.js`

**Interfaces:**
- Produces: 실행 가능한 빈 Expo 앱, `npm test`로 동작하는 Jest 환경.

- [ ] **Step 1: Expo 앱 생성**

현재 폴더(`D:\project\gundex`)에는 `docs/`와 `.git/`만 있다. 이 둘은 Expo 템플릿 파일과 겹치지 않으므로 현재 폴더에 그대로 생성한다.

Run:
```bash
npx create-expo-app@latest . --template blank
```
만약 "directory not empty"로 거부되면, 임시 폴더에 만들고 내용물만 옮긴다:
```bash
npx create-expo-app@latest gundex-tmp --template blank
# gundex-tmp 안의 모든 파일/폴더(.git 제외)를 현재 폴더로 이동한 뒤 gundex-tmp 삭제
```

- [ ] **Step 2: 앱이 켜지는지 확인**

Run: `npx expo start`
Expected: 터미널에 QR 코드가 뜨고 에러 없이 대기 상태가 된다. (확인 후 Ctrl+C로 종료)

- [ ] **Step 3: 네비게이션 + 테스트 패키지 설치**

Run:
```bash
npx expo install @react-navigation/native @react-navigation/native-stack @react-navigation/bottom-tabs react-native-screens react-native-safe-area-context
npx expo install jest jest-expo @testing-library/react-native react-test-renderer
```

- [ ] **Step 4: package.json에 jest 설정 추가**

`package.json`에 `"scripts"`의 `"test"`와 최상위 `"jest"` 블록을 추가한다:
```json
{
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "preset": "jest-expo",
    "transformIgnorePatterns": [
      "node_modules/(?!((jest-)?react-native|@react-native(-community)?|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@unimodules/.*|unimodules|sentry-expo|native-base|react-native-svg))"
    ]
  }
}
```

- [ ] **Step 5: 스모크 테스트 작성**

`smoke.test.js`:
```js
test('jest가 동작한다', () => {
  expect(1 + 1).toBe(2);
});
```

- [ ] **Step 6: 테스트 실행해서 통과 확인**

Run: `npm test`
Expected: PASS (1 test passed)

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "셋업: Expo 앱 + 네비게이션 + Jest 환경"
```

---

### Task 2: 분류 매핑 (categories)

**Files:**
- Create: `data/categories.js`
- Test: `data/categories.test.js`

**Interfaces:**
- Produces:
  - `CATEGORIES`: `Array<{ code: string, label: string }>` (6개)
  - `categoryLabel(code: string): string` — 모르는 코드면 코드 그대로 반환

- [ ] **Step 1: 실패하는 테스트 작성**

`data/categories.test.js`:
```js
import { CATEGORIES, categoryLabel } from './categories';

test('분류 6종이 있다', () => {
  expect(CATEGORIES).toHaveLength(6);
});

test('코드값을 한글 표시값으로 바꾼다', () => {
  expect(categoryLabel('rifle')).toBe('소총');
  expect(categoryLabel('pistol')).toBe('권총');
});

test('모르는 코드는 그대로 반환한다', () => {
  expect(categoryLabel('unknown')).toBe('unknown');
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- categories`
Expected: FAIL ("Cannot find module './categories'")

- [ ] **Step 3: 구현**

`data/categories.js`:
```js
export const CATEGORIES = [
  { code: 'pistol', label: '권총' },
  { code: 'rifle', label: '소총' },
  { code: 'shotgun', label: '산탄총' },
  { code: 'smg', label: '기관단총' },
  { code: 'sniper', label: '저격소총' },
  { code: 'machinegun', label: '기관총' },
];

export function categoryLabel(code) {
  const found = CATEGORIES.find((c) => c.code === code);
  return found ? found.label : code;
}
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- categories`
Expected: PASS (3 tests)

- [ ] **Step 5: 커밋**

```bash
git add data/categories.js data/categories.test.js
git commit -m "데이터: 분류 코드↔한글 매핑 추가"
```

---

### Task 3: 시드 데이터 (guns.json)

**Files:**
- Create: `data/guns.json`
- Test: `data/guns.test.js`

**Interfaces:**
- Produces: 13개 필드를 모두 가진 총기 객체 배열 (시드 8개, 6개 분류 모두 포함).

- [ ] **Step 1: 실패하는 테스트 작성**

`data/guns.test.js`:
```js
import guns from './guns.json';
import { CATEGORIES } from './categories';

const REQUIRED = [
  'id', 'name', 'manufacturer', 'country', 'category', 'caliber',
  'weight', 'length', 'year', 'description', 'imageUrl', 'imageCredit', 'sourceUrl',
];
const VALID_CATEGORIES = CATEGORIES.map((c) => c.code);

test('총기 데이터가 8개 이상 있다', () => {
  expect(guns.length).toBeGreaterThanOrEqual(8);
});

test('모든 총기가 13개 필드를 전부 가진다', () => {
  for (const gun of guns) {
    for (const field of REQUIRED) {
      expect(gun[field] === undefined || gun[field] === '').toBe(false);
    }
  }
});

test('category는 정해진 6종 중 하나다', () => {
  for (const gun of guns) {
    expect(VALID_CATEGORIES).toContain(gun.category);
  }
});

test('id는 중복이 없다', () => {
  const ids = guns.map((g) => g.id);
  expect(new Set(ids).size).toBe(ids.length);
});

test('year는 숫자다', () => {
  for (const gun of guns) {
    expect(typeof gun.year).toBe('number');
  }
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- guns`
Expected: FAIL ("Cannot find module './guns.json'")

- [ ] **Step 3: 시드 데이터 작성**

`data/guns.json`. 이미지 URL은 Wikimedia Commons의 `Special:FilePath` 패턴(현재 파일로 자동 연결)을 쓴다. 파일명이 틀리면 사진만 안 뜨므로, Task 9 이후 앱에서 사진이 안 보이면 Wikimedia Commons에서 정확한 파일명을 찾아 교체한다. `imageCredit`의 `___`(저작자명)은 각 Commons 파일 페이지에서 확인해 채운다.
```json
[
  {
    "id": "ak-47",
    "name": "AK-47",
    "manufacturer": "Kalashnikov Concern",
    "country": "소련 (러시아)",
    "category": "rifle",
    "caliber": "7.62×39mm",
    "weight": "3.47 kg",
    "length": "880 mm",
    "year": 1949,
    "description": "미하일 칼라시니코프가 개발한 돌격소총. 단순한 구조와 높은 내구성으로 세계에서 가장 많이 생산된 소총 중 하나다.",
    "imageUrl": "https://commons.wikimedia.org/wiki/Special:FilePath/AK-47_type_II_Part_DM-ST-89-01131.jpg",
    "imageCredit": "U.S. DoD / Wikimedia Commons, Public domain",
    "sourceUrl": "https://ko.wikipedia.org/wiki/AK-47"
  },
  {
    "id": "m16",
    "name": "M16",
    "manufacturer": "Colt",
    "country": "미국",
    "category": "rifle",
    "caliber": "5.56×45mm",
    "weight": "3.40 kg",
    "length": "1000 mm",
    "year": 1964,
    "description": "미군의 제식 돌격소총. 가볍고 반동이 적으며 다양한 파생형으로 발전했다.",
    "imageUrl": "https://commons.wikimedia.org/wiki/Special:FilePath/M16A1_brimob.jpg",
    "imageCredit": "Wikimedia Commons, Public domain",
    "sourceUrl": "https://ko.wikipedia.org/wiki/M16_(소총)"
  },
  {
    "id": "glock-17",
    "name": "Glock 17",
    "manufacturer": "Glock",
    "country": "오스트리아",
    "category": "pistol",
    "caliber": "9×19mm",
    "weight": "0.625 kg",
    "length": "186 mm",
    "year": 1982,
    "description": "폴리머 프레임을 대중화한 자동권총. 가볍고 신뢰성이 높아 전 세계 경찰·군에서 널리 쓰인다.",
    "imageUrl": "https://commons.wikimedia.org/wiki/Special:FilePath/Glock_17_(white_background).jpg",
    "imageCredit": "Wikimedia Commons, CC BY-SA",
    "sourceUrl": "https://ko.wikipedia.org/wiki/글록_(권총)"
  },
  {
    "id": "m1911",
    "name": "M1911",
    "manufacturer": "Colt",
    "country": "미국",
    "category": "pistol",
    "caliber": ".45 ACP",
    "weight": "1.1 kg",
    "length": "210 mm",
    "year": 1911,
    "description": "존 브라우닝이 설계한 자동권총. 미군 제식으로 오래 사용되었으며 지금도 인기가 높다.",
    "imageUrl": "https://commons.wikimedia.org/wiki/Special:FilePath/M1911_A1_pistol.jpg",
    "imageCredit": "Wikimedia Commons, Public domain",
    "sourceUrl": "https://ko.wikipedia.org/wiki/M1911"
  },
  {
    "id": "remington-870",
    "name": "Remington 870",
    "manufacturer": "Remington Arms",
    "country": "미국",
    "category": "shotgun",
    "caliber": "12 게이지",
    "weight": "3.2 kg",
    "length": "1219 mm",
    "year": 1950,
    "description": "펌프액션 산탄총의 대표작. 사냥·사격·군경용으로 폭넓게 쓰인다.",
    "imageUrl": "https://commons.wikimedia.org/wiki/Special:FilePath/Remington_870.jpg",
    "imageCredit": "Wikimedia Commons, CC BY-SA",
    "sourceUrl": "https://ko.wikipedia.org/wiki/레밍턴_M870"
  },
  {
    "id": "mp5",
    "name": "MP5",
    "manufacturer": "Heckler & Koch",
    "country": "독일",
    "category": "smg",
    "caliber": "9×19mm",
    "weight": "2.54 kg",
    "length": "680 mm",
    "year": 1966,
    "description": "독일 H&K의 기관단총. 높은 명중률과 신뢰성으로 특수부대·경찰이 즐겨 쓴다.",
    "imageUrl": "https://commons.wikimedia.org/wiki/Special:FilePath/Hk-mp5.jpg",
    "imageCredit": "Wikimedia Commons, CC BY-SA",
    "sourceUrl": "https://ko.wikipedia.org/wiki/MP5"
  },
  {
    "id": "barrett-m82",
    "name": "Barrett M82",
    "manufacturer": "Barrett Firearms",
    "country": "미국",
    "category": "sniper",
    "caliber": ".50 BMG",
    "weight": "14 kg",
    "length": "1448 mm",
    "year": 1989,
    "description": "대물 저격소총. .50 BMG 탄을 사용해 장거리 표적과 장비를 무력화하는 용도로 개발되었다.",
    "imageUrl": "https://commons.wikimedia.org/wiki/Special:FilePath/Barrett_M82A1.jpg",
    "imageCredit": "Wikimedia Commons, Public domain",
    "sourceUrl": "https://ko.wikipedia.org/wiki/배럿_M82"
  },
  {
    "id": "m2-browning",
    "name": "M2 Browning",
    "manufacturer": "Browning",
    "country": "미국",
    "category": "machinegun",
    "caliber": ".50 BMG",
    "weight": "38 kg",
    "length": "1654 mm",
    "year": 1933,
    "description": "존 브라우닝이 설계한 중기관총. '50 캘리버'로 불리며 1세기 가까이 현역으로 쓰이고 있다.",
    "imageUrl": "https://commons.wikimedia.org/wiki/Special:FilePath/Machine_gun_M2.jpg",
    "imageCredit": "Wikimedia Commons, Public domain",
    "sourceUrl": "https://ko.wikipedia.org/wiki/M2_브라우닝"
  }
]
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- guns`
Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add data/guns.json data/guns.test.js
git commit -m "데이터: 총기 시드 8종 추가"
```

---

### Task 4: 데이터 접근 함수 (gunData)

**Files:**
- Create: `lib/gunData.js`
- Test: `lib/gunData.test.js`

**Interfaces:**
- Consumes: `data/guns.json`
- Produces:
  - `getAllGuns(): Gun[]`
  - `getGunById(id: string): Gun | undefined`
  - `getGunsByCategory(category: string): Gun[]` — `'all'`이면 전체
  - `searchGuns(query: string): Gun[]` — 이름/제조사 부분일치(대소문자 무시), 빈 문자열이면 전체

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/gunData.test.js`:
```js
import { getAllGuns, getGunById, getGunsByCategory, searchGuns } from './gunData';

test('getAllGuns는 모든 총기를 반환한다', () => {
  expect(getAllGuns().length).toBeGreaterThanOrEqual(8);
});

test('getGunById는 id로 한 개를 찾는다', () => {
  expect(getGunById('ak-47').name).toBe('AK-47');
});

test('getGunById는 없는 id면 undefined', () => {
  expect(getGunById('없는총')).toBeUndefined();
});

test("getGunsByCategory('pistol')는 권총만 반환한다", () => {
  const result = getGunsByCategory('pistol');
  expect(result.length).toBeGreaterThan(0);
  expect(result.every((g) => g.category === 'pistol')).toBe(true);
});

test("getGunsByCategory('all')는 전체를 반환한다", () => {
  expect(getGunsByCategory('all').length).toBe(getAllGuns().length);
});

test('searchGuns는 이름 부분일치(대소문자 무시)로 찾는다', () => {
  const result = searchGuns('ak');
  expect(result.some((g) => g.id === 'ak-47')).toBe(true);
});

test('searchGuns는 제조사로도 찾는다', () => {
  const result = searchGuns('glock');
  expect(result.some((g) => g.id === 'glock-17')).toBe(true);
});

test('searchGuns는 빈 문자열이면 전체를 반환한다', () => {
  expect(searchGuns('   ').length).toBe(getAllGuns().length);
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- gunData`
Expected: FAIL ("Cannot find module './gunData'")

- [ ] **Step 3: 구현**

`lib/gunData.js`:
```js
import guns from '../data/guns.json';

export function getAllGuns() {
  return guns;
}

export function getGunById(id) {
  return guns.find((gun) => gun.id === id);
}

export function getGunsByCategory(category) {
  if (category === 'all') return guns;
  return guns.filter((gun) => gun.category === category);
}

export function searchGuns(query) {
  const q = query.trim().toLowerCase();
  if (q === '') return guns;
  return guns.filter(
    (gun) =>
      gun.name.toLowerCase().includes(q) ||
      gun.manufacturer.toLowerCase().includes(q)
  );
}
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- gunData`
Expected: PASS (8 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/gunData.js lib/gunData.test.js
git commit -m "로직: 데이터 접근 함수(전체/단건/분류/검색) 추가"
```

---

### Task 5: 비교함 순수 로직 (compareLogic)

**Files:**
- Create: `lib/compareLogic.js`
- Test: `lib/compareLogic.test.js`

**Interfaces:**
- Produces:
  - `isInList(list: Gun[], id: string): boolean`
  - `addToList(list: Gun[], gun: Gun): Gun[]` — 새 배열 반환. 이미 있거나 2개가 차 있으면 원본과 같은 내용 반환(추가 안 됨)
  - `removeFromList(list: Gun[], id: string): Gun[]`

- [ ] **Step 1: 실패하는 테스트 작성**

`lib/compareLogic.test.js`:
```js
import { isInList, addToList, removeFromList } from './compareLogic';

const a = { id: 'a', name: 'A' };
const b = { id: 'b', name: 'B' };
const c = { id: 'c', name: 'C' };

test('isInList는 포함 여부를 알려준다', () => {
  expect(isInList([a], 'a')).toBe(true);
  expect(isInList([a], 'b')).toBe(false);
});

test('addToList는 새 총을 추가한다', () => {
  expect(addToList([a], b)).toEqual([a, b]);
});

test('addToList는 이미 있으면 추가하지 않는다', () => {
  expect(addToList([a], a)).toEqual([a]);
});

test('addToList는 2개가 차면 추가하지 않는다', () => {
  expect(addToList([a, b], c)).toEqual([a, b]);
});

test('addToList는 원본 배열을 바꾸지 않는다', () => {
  const list = [a];
  addToList(list, b);
  expect(list).toEqual([a]);
});

test('removeFromList는 id로 제거한다', () => {
  expect(removeFromList([a, b], 'a')).toEqual([b]);
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- compareLogic`
Expected: FAIL ("Cannot find module './compareLogic'")

- [ ] **Step 3: 구현**

`lib/compareLogic.js`:
```js
export function isInList(list, id) {
  return list.some((gun) => gun.id === id);
}

export function addToList(list, gun) {
  if (isInList(list, gun.id)) return list;
  if (list.length >= 2) return list;
  return [...list, gun];
}

export function removeFromList(list, id) {
  return list.filter((gun) => gun.id !== id);
}
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- compareLogic`
Expected: PASS (6 tests)

- [ ] **Step 5: 커밋**

```bash
git add lib/compareLogic.js lib/compareLogic.test.js
git commit -m "로직: 비교함 순수 로직(담기/빼기/포함, 최대 2) 추가"
```

---

### Task 6: 비교함 Context (CompareContext)

**Files:**
- Create: `context/CompareContext.js`
- Test: `context/CompareContext.test.js`

**Interfaces:**
- Consumes: `lib/compareLogic.js`
- Produces:
  - `<CompareProvider>{children}</CompareProvider>`
  - `useCompare(): { compareList, addToCompare(gun) => boolean, removeFromCompare(id), clearCompare(), isInCompare(id) }`
  - `addToCompare`는 실제로 담겼으면 `true`, 안 담겼으면(이미 있거나 가득) `false` 반환.

- [ ] **Step 1: 실패하는 테스트 작성**

`context/CompareContext.test.js`:
```js
import { render, screen, fireEvent } from '@testing-library/react-native';
import { Text, Button } from 'react-native';
import { CompareProvider, useCompare } from './CompareContext';

const a = { id: 'a', name: 'A' };
const b = { id: 'b', name: 'B' };
const c = { id: 'c', name: 'C' };

function Harness() {
  const { compareList, addToCompare, removeFromCompare, clearCompare } = useCompare();
  return (
    <>
      <Text testID="count">{compareList.length}</Text>
      <Button title="add-a" onPress={() => addToCompare(a)} />
      <Button title="add-b" onPress={() => addToCompare(b)} />
      <Button title="add-c" onPress={() => addToCompare(c)} />
      <Button title="remove-a" onPress={() => removeFromCompare('a')} />
      <Button title="clear" onPress={() => clearCompare()} />
    </>
  );
}

function renderHarness() {
  render(
    <CompareProvider>
      <Harness />
    </CompareProvider>
  );
}

test('총을 담으면 개수가 늘어난다', () => {
  renderHarness();
  fireEvent.press(screen.getByText('add-a'));
  expect(screen.getByTestId('count').props.children).toBe(1);
});

test('최대 2개까지만 담긴다', () => {
  renderHarness();
  fireEvent.press(screen.getByText('add-a'));
  fireEvent.press(screen.getByText('add-b'));
  fireEvent.press(screen.getByText('add-c'));
  expect(screen.getByTestId('count').props.children).toBe(2);
});

test('빼기와 비우기가 동작한다', () => {
  renderHarness();
  fireEvent.press(screen.getByText('add-a'));
  fireEvent.press(screen.getByText('add-b'));
  fireEvent.press(screen.getByText('remove-a'));
  expect(screen.getByTestId('count').props.children).toBe(1);
  fireEvent.press(screen.getByText('clear'));
  expect(screen.getByTestId('count').props.children).toBe(0);
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- CompareContext`
Expected: FAIL ("Cannot find module './CompareContext'")

- [ ] **Step 3: 구현**

`context/CompareContext.js`:
```js
import { createContext, useContext, useState } from 'react';
import { addToList, removeFromList, isInList } from '../lib/compareLogic';

const CompareContext = createContext(null);

export function CompareProvider({ children }) {
  const [compareList, setCompareList] = useState([]);

  const addToCompare = (gun) => {
    const next = addToList(compareList, gun);
    const added = next.length !== compareList.length;
    if (added) setCompareList(next);
    return added;
  };

  const removeFromCompare = (id) =>
    setCompareList((list) => removeFromList(list, id));

  const clearCompare = () => setCompareList([]);

  const isInCompare = (id) => isInList(compareList, id);

  return (
    <CompareContext.Provider
      value={{ compareList, addToCompare, removeFromCompare, clearCompare, isInCompare }}
    >
      {children}
    </CompareContext.Provider>
  );
}

export function useCompare() {
  return useContext(CompareContext);
}
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- CompareContext`
Expected: PASS (3 tests)

- [ ] **Step 5: 커밋**

```bash
git add context/CompareContext.js context/CompareContext.test.js
git commit -m "상태: 비교함 Context(useCompare) 추가"
```

---

### Task 7: 카드 컴포넌트 (GunCard)

**Files:**
- Create: `components/GunCard.js`
- Test: `components/GunCard.test.js`

**Interfaces:**
- Consumes: `data/categories.js` (`categoryLabel`)
- Produces: `<GunCard gun={Gun} onPress={() => void} />` — 썸네일 + 이름 + "분류 · 구경" 표시. 탭 가능 영역의 `testID="gun-card"`.

- [ ] **Step 1: 실패하는 테스트 작성**

`components/GunCard.test.js`:
```js
import { render, screen, fireEvent } from '@testing-library/react-native';
import GunCard from './GunCard';

const gun = {
  id: 'ak-47',
  name: 'AK-47',
  category: 'rifle',
  caliber: '7.62×39mm',
  imageUrl: 'https://example.com/x.jpg',
};

test('이름과 분류·구경을 보여준다', () => {
  render(<GunCard gun={gun} onPress={() => {}} />);
  expect(screen.getByText('AK-47')).toBeTruthy();
  expect(screen.getByText('소총 · 7.62×39mm')).toBeTruthy();
});

test('누르면 onPress가 호출된다', () => {
  const onPress = jest.fn();
  render(<GunCard gun={gun} onPress={onPress} />);
  fireEvent.press(screen.getByTestId('gun-card'));
  expect(onPress).toHaveBeenCalled();
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- GunCard`
Expected: FAIL ("Cannot find module './GunCard'")

- [ ] **Step 3: 구현**

`components/GunCard.js`:
```js
import { View, Text, Image, TouchableOpacity, StyleSheet } from 'react-native';
import { categoryLabel } from '../data/categories';

export default function GunCard({ gun, onPress }) {
  return (
    <TouchableOpacity testID="gun-card" style={styles.card} onPress={onPress}>
      <Image source={{ uri: gun.imageUrl }} style={styles.thumb} />
      <View style={styles.info}>
        <Text style={styles.name}>{gun.name}</Text>
        <Text style={styles.meta}>
          {categoryLabel(gun.category)} · {gun.caliber}
        </Text>
      </View>
    </TouchableOpacity>
  );
}

const styles = StyleSheet.create({
  card: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 12,
    borderBottomWidth: 1,
    borderBottomColor: '#eee',
  },
  thumb: { width: 64, height: 64, borderRadius: 8, backgroundColor: '#ddd' },
  info: { marginLeft: 12, flex: 1 },
  name: { fontSize: 16, fontWeight: 'bold' },
  meta: { fontSize: 13, color: '#666', marginTop: 4 },
});
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- GunCard`
Expected: PASS (2 tests)

- [ ] **Step 5: 커밋**

```bash
git add components/GunCard.js components/GunCard.test.js
git commit -m "UI: 총기 카드 컴포넌트 추가"
```

---

### Task 8: 목록·검색 화면 + 네비게이션 조립

**Files:**
- Create: `screens/ListScreen.js`, `screens/SearchScreen.js`
- Modify: `App.js` (전체 교체)
- Test: `screens/ListScreen.test.js`, `screens/SearchScreen.test.js`

**Interfaces:**
- Consumes: `lib/gunData.js`, `data/categories.js`, `components/GunCard.js`, `context/CompareContext.js`
- Produces: `ListScreen`, `SearchScreen` (둘 다 `navigation` prop을 받아 카드 탭 시 `navigation.navigate('Detail', { id })` 호출). `App.js`가 탭(도감/검색/비교) + 상세를 가진 루트 스택을 `CompareProvider`로 감싼다.

- [ ] **Step 1: 실패하는 테스트 작성 (ListScreen)**

`screens/ListScreen.test.js`:
```js
import { render, screen, fireEvent } from '@testing-library/react-native';
import ListScreen from './ListScreen';

const navigation = { navigate: jest.fn() };

test('처음엔 전체 목록이 보인다 (AK-47 포함)', () => {
  render(<ListScreen navigation={navigation} />);
  expect(screen.getByText('AK-47')).toBeTruthy();
});

test("'권총' 필터를 누르면 권총만 보인다", () => {
  render(<ListScreen navigation={navigation} />);
  fireEvent.press(screen.getByText('권총'));
  expect(screen.getByText('Glock 17')).toBeTruthy();
  expect(screen.queryByText('AK-47')).toBeNull();
});

test('카드를 누르면 상세로 이동한다', () => {
  render(<ListScreen navigation={navigation} />);
  fireEvent.press(screen.getByText('AK-47'));
  expect(navigation.navigate).toHaveBeenCalledWith('Detail', { id: 'ak-47' });
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- ListScreen`
Expected: FAIL ("Cannot find module './ListScreen'")

- [ ] **Step 3: 구현 (ListScreen)**

`screens/ListScreen.js`:
```js
import { useState } from 'react';
import { View, FlatList, TouchableOpacity, Text, StyleSheet } from 'react-native';
import { getGunsByCategory } from '../lib/gunData';
import { CATEGORIES } from '../data/categories';
import GunCard from '../components/GunCard';

const FILTERS = [{ code: 'all', label: '전체' }, ...CATEGORIES];

export default function ListScreen({ navigation }) {
  const [selected, setSelected] = useState('all');
  const guns = getGunsByCategory(selected);

  return (
    <View style={styles.container}>
      <View style={styles.filters}>
        {FILTERS.map((f) => (
          <TouchableOpacity key={f.code} onPress={() => setSelected(f.code)}>
            <Text style={[styles.filter, selected === f.code && styles.filterActive]}>
              {f.label}
            </Text>
          </TouchableOpacity>
        ))}
      </View>
      <FlatList
        data={guns}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <GunCard gun={item} onPress={() => navigation.navigate('Detail', { id: item.id })} />
        )}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#fff' },
  filters: { flexDirection: 'row', flexWrap: 'wrap', padding: 8, gap: 8 },
  filter: {
    paddingHorizontal: 12,
    paddingVertical: 6,
    borderRadius: 16,
    backgroundColor: '#eee',
    fontSize: 13,
  },
  filterActive: { backgroundColor: '#333', color: '#fff' },
});
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- ListScreen`
Expected: PASS (3 tests)

- [ ] **Step 5: 실패하는 테스트 작성 (SearchScreen)**

`screens/SearchScreen.test.js`:
```js
import { render, screen, fireEvent } from '@testing-library/react-native';
import SearchScreen from './SearchScreen';

const navigation = { navigate: jest.fn() };

test('검색어를 입력하면 일치하는 총만 보인다', () => {
  render(<SearchScreen navigation={navigation} />);
  fireEvent.changeText(screen.getByPlaceholderText('총기 이름 검색'), 'glock');
  expect(screen.getByText('Glock 17')).toBeTruthy();
  expect(screen.queryByText('AK-47')).toBeNull();
});

test('결과 카드를 누르면 상세로 이동한다', () => {
  render(<SearchScreen navigation={navigation} />);
  fireEvent.changeText(screen.getByPlaceholderText('총기 이름 검색'), 'glock');
  fireEvent.press(screen.getByText('Glock 17'));
  expect(navigation.navigate).toHaveBeenCalledWith('Detail', { id: 'glock-17' });
});
```

- [ ] **Step 6: 테스트 실행 (실패 확인)**

Run: `npm test -- SearchScreen`
Expected: FAIL ("Cannot find module './SearchScreen'")

- [ ] **Step 7: 구현 (SearchScreen)**

`screens/SearchScreen.js`:
```js
import { useState } from 'react';
import { View, TextInput, FlatList, StyleSheet } from 'react-native';
import { searchGuns } from '../lib/gunData';
import GunCard from '../components/GunCard';

export default function SearchScreen({ navigation }) {
  const [query, setQuery] = useState('');
  const results = searchGuns(query);

  return (
    <View style={styles.container}>
      <TextInput
        style={styles.input}
        placeholder="총기 이름 검색"
        value={query}
        onChangeText={setQuery}
      />
      <FlatList
        data={results}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <GunCard gun={item} onPress={() => navigation.navigate('Detail', { id: item.id })} />
        )}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#fff' },
  input: {
    margin: 12,
    padding: 10,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 8,
    fontSize: 15,
  },
});
```

- [ ] **Step 8: 테스트 실행 (통과 확인)**

Run: `npm test -- SearchScreen`
Expected: PASS (2 tests)

- [ ] **Step 9: App.js 교체 (네비게이션 조립)**

`App.js` 전체를 아래로 교체한다. (DetailScreen/CompareScreen은 Task 9·10에서 만들므로, 지금은 import 대상이 존재하도록 두 파일을 임시 최소 구현으로 먼저 만든 뒤 교체해도 되지만, 여기서는 Task 9·10 완료 후 최종본을 둔다. 지금 단계에서 앱을 켜보려면 DetailScreen/CompareScreen 줄을 잠시 주석 처리하고 해당 Tab.Screen/Stack.Screen도 주석 처리한다.)
```js
import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import { CompareProvider } from './context/CompareContext';
import ListScreen from './screens/ListScreen';
import SearchScreen from './screens/SearchScreen';
import CompareScreen from './screens/CompareScreen';
import DetailScreen from './screens/DetailScreen';

const Tab = createBottomTabNavigator();
const Stack = createNativeStackNavigator();

function Tabs() {
  return (
    <Tab.Navigator>
      <Tab.Screen name="도감" component={ListScreen} />
      <Tab.Screen name="검색" component={SearchScreen} />
      <Tab.Screen name="비교" component={CompareScreen} />
    </Tab.Navigator>
  );
}

export default function App() {
  return (
    <CompareProvider>
      <NavigationContainer>
        <Stack.Navigator>
          <Stack.Screen name="Tabs" component={Tabs} options={{ headerShown: false }} />
          <Stack.Screen name="Detail" component={DetailScreen} options={{ title: '상세 정보' }} />
        </Stack.Navigator>
      </NavigationContainer>
    </CompareProvider>
  );
}
```

- [ ] **Step 10: 커밋**

```bash
git add screens/ListScreen.js screens/ListScreen.test.js screens/SearchScreen.js screens/SearchScreen.test.js App.js
git commit -m "UI: 목록·검색 화면 + 네비게이션 조립"
```

---

### Task 9: 상세 화면 (DetailScreen)

**Files:**
- Create: `screens/DetailScreen.js`
- Test: `screens/DetailScreen.test.js`

**Interfaces:**
- Consumes: `lib/gunData.js` (`getGunById`), `data/categories.js` (`categoryLabel`), `context/CompareContext.js` (`useCompare`)
- Produces: `DetailScreen` — `route.params.id`로 한 개를 표시(사진/제원/설명/사진출처). "더 알아보기"는 `Linking.openURL(sourceUrl)`, "비교함에 담기"는 `addToCompare(gun)` 호출 후 실패 시 `Alert`.

- [ ] **Step 1: 실패하는 테스트 작성**

`screens/DetailScreen.test.js`:
```js
import { render, screen } from '@testing-library/react-native';
import { CompareProvider } from '../context/CompareContext';
import DetailScreen from './DetailScreen';

function renderDetail(id) {
  render(
    <CompareProvider>
      <DetailScreen route={{ params: { id } }} />
    </CompareProvider>
  );
}

test('총기 이름과 제원을 보여준다', () => {
  renderDetail('ak-47');
  expect(screen.getByText('AK-47')).toBeTruthy();
  expect(screen.getByText('7.62×39mm')).toBeTruthy();
  expect(screen.getByText('3.47 kg')).toBeTruthy();
});

test('분류는 한글로 보여준다', () => {
  renderDetail('ak-47');
  expect(screen.getByText('소총')).toBeTruthy();
});

test('없는 id면 안내 문구를 보여준다', () => {
  renderDetail('없는총');
  expect(screen.getByText('총기 정보를 찾을 수 없어요.')).toBeTruthy();
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- DetailScreen`
Expected: FAIL ("Cannot find module './DetailScreen'")

- [ ] **Step 3: 구현**

`screens/DetailScreen.js`:
```js
import {
  ScrollView, View, Text, Image, Button, StyleSheet, Linking, Alert,
} from 'react-native';
import { getGunById } from '../lib/gunData';
import { categoryLabel } from '../data/categories';
import { useCompare } from '../context/CompareContext';

function Row({ label, value }) {
  return (
    <View style={styles.row}>
      <Text style={styles.rowLabel}>{label}</Text>
      <Text style={styles.rowValue}>{value}</Text>
    </View>
  );
}

export default function DetailScreen({ route }) {
  const { id } = route.params;
  const gun = getGunById(id);
  const { addToCompare } = useCompare();

  if (!gun) {
    return (
      <View style={styles.center}>
        <Text>총기 정보를 찾을 수 없어요.</Text>
      </View>
    );
  }

  const handleAdd = () => {
    const added = addToCompare(gun);
    if (!added) {
      Alert.alert('담지 못했어요', '이미 담겼거나 비교함이 가득 찼어요 (최대 2개).');
    } else {
      Alert.alert('비교함에 담았어요', '비교 탭에서 확인하세요.');
    }
  };

  return (
    <ScrollView style={styles.container} contentContainerStyle={styles.content}>
      <Image source={{ uri: gun.imageUrl }} style={styles.image} />
      <Text style={styles.title}>{gun.name}</Text>
      <Row label="분류" value={categoryLabel(gun.category)} />
      <Row label="제조사" value={gun.manufacturer} />
      <Row label="제조국" value={gun.country} />
      <Row label="구경" value={gun.caliber} />
      <Row label="무게" value={gun.weight} />
      <Row label="길이" value={gun.length} />
      <Row label="연도" value={String(gun.year)} />
      <Text style={styles.desc}>{gun.description}</Text>
      <Text style={styles.credit}>사진 출처: {gun.imageCredit}</Text>
      <View style={styles.buttons}>
        <Button title="더 알아보기" onPress={() => Linking.openURL(gun.sourceUrl)} />
        <Button title="비교함에 담기" onPress={handleAdd} />
      </View>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#fff' },
  content: { padding: 16 },
  center: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  image: { width: '100%', height: 200, borderRadius: 8, backgroundColor: '#ddd' },
  title: { fontSize: 24, fontWeight: 'bold', marginVertical: 12 },
  row: { flexDirection: 'row', paddingVertical: 6, borderBottomWidth: 1, borderBottomColor: '#f0f0f0' },
  rowLabel: { width: 80, color: '#666' },
  rowValue: { flex: 1, fontWeight: '500' },
  desc: { marginTop: 16, lineHeight: 22 },
  credit: { marginTop: 12, fontSize: 12, color: '#999' },
  buttons: { marginTop: 16, gap: 8 },
});
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- DetailScreen`
Expected: PASS (3 tests)

- [ ] **Step 5: 커밋**

```bash
git add screens/DetailScreen.js screens/DetailScreen.test.js
git commit -m "UI: 상세 화면(제원/설명/출처/비교 담기) 추가"
```

---

### Task 10: 비교 화면 (CompareScreen) + 통합 확인

**Files:**
- Create: `screens/CompareScreen.js`
- Test: `screens/CompareScreen.test.js`

**Interfaces:**
- Consumes: `data/categories.js` (`categoryLabel`), `context/CompareContext.js` (`useCompare`)
- Produces: `CompareScreen` — 비교함이 2개 미만이면 안내 문구, 2개면 제원을 같은 줄에 나란히 표시. "비교함 비우기" 버튼.

- [ ] **Step 1: 실패하는 테스트 작성**

`screens/CompareScreen.test.js`:
```js
import { render, screen, fireEvent } from '@testing-library/react-native';
import { Button } from 'react-native';
import { CompareProvider, useCompare } from '../context/CompareContext';
import CompareScreen from './CompareScreen';

const ak = { id: 'ak-47', name: 'AK-47', category: 'rifle', caliber: '7.62×39mm', weight: '3.47 kg', length: '880 mm', year: 1949, country: '소련 (러시아)', imageUrl: 'x' };
const glock = { id: 'glock-17', name: 'Glock 17', category: 'pistol', caliber: '9×19mm', weight: '0.625 kg', length: '186 mm', year: 1982, country: '오스트리아', imageUrl: 'x' };

function Seeder({ guns }) {
  const { addToCompare } = useCompare();
  return <Button title="seed" onPress={() => guns.forEach((g) => addToCompare(g))} />;
}

function renderWith(guns) {
  render(
    <CompareProvider>
      <Seeder guns={guns} />
      <CompareScreen />
    </CompareProvider>
  );
  fireEvent.press(screen.getByText('seed'));
}

test('2개 미만이면 안내 문구를 보여준다', () => {
  render(
    <CompareProvider>
      <CompareScreen />
    </CompareProvider>
  );
  expect(screen.getByText(/비교할 총기를 2개/)).toBeTruthy();
});

test('2개면 두 총의 이름과 제원을 나란히 보여준다', () => {
  renderWith([ak, glock]);
  expect(screen.getByText('AK-47')).toBeTruthy();
  expect(screen.getByText('Glock 17')).toBeTruthy();
  expect(screen.getByText('7.62×39mm')).toBeTruthy();
  expect(screen.getByText('9×19mm')).toBeTruthy();
});
```

- [ ] **Step 2: 테스트 실행 (실패 확인)**

Run: `npm test -- CompareScreen`
Expected: FAIL ("Cannot find module './CompareScreen'")

- [ ] **Step 3: 구현**

`screens/CompareScreen.js`:
```js
import { View, Text, Image, ScrollView, Button, StyleSheet } from 'react-native';
import { categoryLabel } from '../data/categories';
import { useCompare } from '../context/CompareContext';

const SPECS = [
  { label: '분류', format: (g) => categoryLabel(g.category) },
  { label: '구경', format: (g) => g.caliber },
  { label: '무게', format: (g) => g.weight },
  { label: '길이', format: (g) => g.length },
  { label: '연도', format: (g) => String(g.year) },
  { label: '제조국', format: (g) => g.country },
];

export default function CompareScreen() {
  const { compareList, clearCompare } = useCompare();

  if (compareList.length < 2) {
    return (
      <View style={styles.center}>
        <Text>비교할 총기를 2개 담아주세요. (현재 {compareList.length}개)</Text>
      </View>
    );
  }

  const [a, b] = compareList;

  return (
    <ScrollView style={styles.container} contentContainerStyle={styles.content}>
      <View style={styles.headerRow}>
        <View style={styles.col}>
          <Image source={{ uri: a.imageUrl }} style={styles.thumb} />
          <Text style={styles.name}>{a.name}</Text>
        </View>
        <View style={styles.col}>
          <Image source={{ uri: b.imageUrl }} style={styles.thumb} />
          <Text style={styles.name}>{b.name}</Text>
        </View>
      </View>
      {SPECS.map((spec) => (
        <View key={spec.label} style={styles.row}>
          <Text style={styles.label}>{spec.label}</Text>
          <Text style={styles.val}>{spec.format(a)}</Text>
          <Text style={styles.val}>{spec.format(b)}</Text>
        </View>
      ))}
      <View style={styles.clear}>
        <Button title="비교함 비우기" onPress={clearCompare} />
      </View>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#fff' },
  content: { padding: 12 },
  center: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: 24 },
  headerRow: { flexDirection: 'row' },
  col: { flex: 1, alignItems: 'center' },
  thumb: { width: 80, height: 80, borderRadius: 8, backgroundColor: '#ddd' },
  name: { fontWeight: 'bold', marginTop: 6, textAlign: 'center' },
  row: { flexDirection: 'row', paddingVertical: 8, borderBottomWidth: 1, borderBottomColor: '#f0f0f0' },
  label: { width: 64, color: '#666' },
  val: { flex: 1, textAlign: 'center' },
  clear: { marginTop: 16 },
});
```

- [ ] **Step 4: 테스트 실행 (통과 확인)**

Run: `npm test -- CompareScreen`
Expected: PASS (2 tests)

- [ ] **Step 5: 전체 테스트 실행**

Run: `npm test`
Expected: 모든 테스트 PASS

- [ ] **Step 6: 앱 수동 확인**

Run: `npx expo start` 후 폰(Expo Go) 또는 웹(`w` 키)에서:
- 도감 탭: 분류 필터를 눌러 목록이 바뀌는지
- 카드 탭 → 상세 화면 진입, 사진/제원/설명 표시 확인 (사진이 안 뜨면 `data/guns.json`의 해당 `imageUrl` 파일명을 Wikimedia Commons에서 확인해 교체)
- 상세에서 "더 알아보기" → 위키 링크 열림, "비교함에 담기" → 알림 표시
- 서로 다른 두 총을 담고 비교 탭에서 나란히 보이는지, "비교함 비우기" 동작 확인

- [ ] **Step 7: 커밋**

```bash
git add screens/CompareScreen.js screens/CompareScreen.test.js
git commit -m "UI: 비교 화면(제원 나란히 비교) 추가"
```

---

## 완료 기준

- `npm test` 전체 통과
- 도감/검색/상세/비교 4개 기능이 앱에서 실제로 동작
- 친구에게 웹 링크(`npx expo start` → 웹) 또는 APK로 공유 가능

## 다음 단계 (이번 범위 밖, 나중에)

- 위키 API 연동: `lib/gunData.js` 함수 내부만 API 호출로 교체 (화면은 안 건드림)
- 데이터 더 추가, 즐겨찾기, 정렬, 다크모드, 영어 버전
